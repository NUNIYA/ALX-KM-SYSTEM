{
  "name": "alx-km-portal-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "install-frontend": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "cd backend && npm start"
  }
}
